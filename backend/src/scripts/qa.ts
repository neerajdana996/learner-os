// `pnpm qa <topicId>` — content QA tool. Implemented in T-024.
const topicId = process.argv[2];
console.error(`qa: not implemented yet (topicId=${topicId ?? '<missing>'}) — see docs/tasks.md T-024`);
process.exit(1);
