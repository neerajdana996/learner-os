# Required for `pnpm --filter <pkg> deploy <dir>` (used to package the
# backend for deployment): copies workspace packages (@learnos/shared,
# @learnos/ui) into node_modules instead of symlinking them, so a deployed
# package is genuinely self-contained and works outside this monorepo.
inject-workspace-packages=true
