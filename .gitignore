# One repo: three apps, two shared packages, and the docs that govern them.
node_modules/
dist/
.output/
.wxt/
.turbo/
.env
*.log
.DS_Store

# Playwright (T-134): the report and the videos are build output, not source.
e2e/report/
e2e/.artifacts/
test-results/
playwright-report/
e2e/matrix-data.json
e2e/fresh-user.json
e2e/diagnostic-users.json
e2e/session-users.json
e2e/map-user.json
.vercel
.env*

# pnpm store, created when pnpm runs with the repo root as its store dir
# (a container install through the bind mount does this). Never committed.
.pnpm-store/
